### /init: "-1" means that value is not used

POST http://{{host}}/init
Content-Type: application/json

{
  "testId": "test",
  "testLengthSeconds": -1,
  "msgsPerSecond": -1,
  "msgSizeBytes": -1,
  "batchSizeSend": -1,
  "maxSendInFlight": -1,
  "batchSizeReceive": -1,
  "mqConfig": {
    "host": "{{mqConfigHost}}",
    "port": "{{mqConfigPort}}",
    "table": "{{mqConfigTable}}",
    "database":"{{mqConfigDatabase}}",
    "username": "{{mqConfigUsername}}",
    "password": "{{mqConfigPassword}}",
    "sslEnabled": "{{sslEnabled}}"
  }
}

### /sender: "-1" means that value is not used

POST http://{{host}}/start/sender
Content-Type: application/json

{
  "testId": "test",
  "testLengthSeconds": 10,
  "msgsPerSecond": 100,
  "msgSizeBytes": 1000,
  "batchSizeSend": 10,
  "maxSendInFlight": 1000,
  "batchSizeReceive": -1,
  "mqConfig": {
    "host": "{{mqConfigHost}}",
    "port": "{{mqConfigPort}}",
    "table": "{{mqConfigTable}}",
    "database":"{{mqConfigDatabase}}",
    "username": "{{mqConfigUsername}}",
    "password": "{{mqConfigPassword}}",
    "senderPoolSize": "{{mqConfigSenderPoolSize}}",
    "sslEnabled": "{{sslEnabled}}"
  }
}

### /receiver: "-1" means that value is not used

POST http://{{host}}/start/receiver
Content-Type: application/json

{
  "testId": "test",
  "testLengthSeconds": -1,
  "msgsPerSecond": -1,
  "msgSizeBytes": -1,
  "batchSizeSend": -1,
  "maxSendInFlight": -1,
  "batchSizeReceive": 10,
  "mqConfig": {
    "host": "{{mqConfigHost}}",
    "port": "{{mqConfigPort}}",
    "table": "{{mqConfigTable}}",
    "database":"{{mqConfigDatabase}}",
    "username": "{{mqConfigUsername}}",
    "password": "{{mqConfigPassword}}",
    "receiverPoolSize": "{{mqConfigReceiverPoolSize}}",
    "sslEnabled": "{{sslEnabled}}"
  }
}

### /cleanup:  "-1" means that value is not used

POST http://{{host}}/cleanup
Content-Type: application/json

{
  "testId": "test",
  "testLengthSeconds": -1,
  "msgsPerSecond": -1,
  "msgSizeBytes": -1,
  "batchSizeSend": -1,
  "maxSendInFlight": -1,
  "batchSizeReceive": -1,
  "mqConfig": {
    "host": "{{mqConfigHost}}",
    "port": "{{mqConfigPort}}",
    "table": "{{mqConfigTable}}",
    "database":"{{mqConfigDatabase}}",
    "username": "{{mqConfigUsername}}",
    "password": "{{mqConfigPassword}}",
    "sslEnabled": "{{sslEnabled}}"
  }
}

### /in-progress

GET http://{{host}}/in-progress

### /metrics

GET http://{{host}}/metrics
